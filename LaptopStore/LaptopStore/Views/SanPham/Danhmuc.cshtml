@using LaptopStore.Models.Entities;
@model IEnumerable<DanhMuc>

<section class="sky-form">
    <h4>Hãng</h4>
    <div class="row row1 scroll-pane">
        <div class="col col-4">
            @{
                foreach (DanhMuc i in Model)
                {
                    @* <label class="checkbox" href="/Sanpham/SPViewDM/@i.ID_DM">@i.tendanhmuc</label>/*@
                    <label class="checkbox"><a href="/Sanpham/SPViewDM/@i.DanhmucID"> @i.Tendanhmuc</a></label>
                }
            }
        </div>
</section>

